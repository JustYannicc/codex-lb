# Regular composition of PR2306, PR2307 and PR2310

This local candidate extends exact predecessor `4586507fbdc59584358535125e948a0aa04fcddc`. The manifest records all 37 selected sources and 11 retained repairs. Apply the PR-only deltas from base `0f6a31c56ac30804ca1c0fac27ca02c6f59bf2b0`; unrelated upstream history remains excluded.

PR2306 adds touched active-folder OpenSpec validation while preserving the existing native workflow test selection. PR2307 adds online migration progress and CLI logging. Its sole conflict was the canonical migration spec EOF: preserve every existing requirement and append its progress requirement once. PR2310 preserves the exact reviewed AGENTS blob. README inventory comes from `b82eefe244703ac208dc346368713b845cf91e45`, updated for the three additions and existing ERR repair.

No migration version file or proxy code changes in this composition. Existing pins, migration hashes and ERR repair bytes remain unchanged. For example, a populated database upgrading through pending revisions must log execution progress and retain its rows; a current database must emit no revision execution events.

The immutable operations receipt under `reports/composition-2306-2307-2310/receipt.md` binds candidate, tree, exact manifest, actual event range, test results, independent review and cleanup. Hosted source checks are separate from aggregate validation. Branch publication does not trigger this workflow's main-only push CI. Publication, image build and live acceptance belong to the registered deployment worker. This composition is not yet deployed or approved for upstream merge.
