# CLI successor composition

This local candidate extends exact frozen007540786dc3df71c6dc2d9a96a8c03f4d9456ed with bounded PR2323 f39b4a3e853cb828421b00d2ac19d4e228f473c5 and PR2325 b609a47c8efcdb9e6431614629beffd9cb278e58. The manifest retains all prior38 source pins,11 repairs, the2286 supplement and deployed migration contract, and adds these two sources. No unrelated upstream branch history is imported.

Both runtime-portability append conflicts were resolved by retaining both complete requirements/context blocks. The CLI retains Desktop relay command/listener registration alongside metadata-mismatches, repair-metadata and retag. Navigation retains Desktop pooled usage/history and both session guides.

Targeted repair scans active and archived first-line metadata up to1MiB, accepts explicit selected IDs and reports byte/item progress with session-metadata-repair backups. Whole-home retag scans sessions only with64KiB leading canonical/legacy discovery, reports its own phase/total schema and retains its backup layout. These are deliberately separate contracts. For example, an archived session is eligible for targeted repair but is outside whole-home JSONL discovery. Opaque transcript tails and unrelated configuration remain protected by each source's tests.

The immutable operations receipt reports/composition-cli-2323-2325/receipt.md binds the exact candidate/tree, installed-CLI disposable-home proof, stale identity/backup/WAL/closed-pipe controls, required checks, event-range OpenSpec, independent review and cleanup. No real Codex home, publication, image build, live app or deployment packet is changed. Source proof does not establish live-home or Windows acceptance.
