The runtime refresh is local composition, with upstream change specs retaining ownership of each feature. For example, a superseded quarantine PR is replaced by its two current proxy carriers and independent test carrier, while an upstream-merged usage fix stops being a separate pin. The source manifest records this substitution so later refreshes do not replay both histories.

The active pooled Desktop task owns its source. Integration uses a frozen accepted implementation and verifies existing identity, quota, relay and Astra behavior. The older managed deployment manifest is stale and its held updater must not replace this runtime.

Tests use disposable SQLite state only. A failed merge, test, build or rehearsal leaves the accepted live runtime intact. Cutover and cleanup belong to the deployment owner after exact candidate verification.

The refresh also composes bounded defect corrections: drain upgrades return retryable HTTP 503; a proxy-injected direct WebSocket anchor rejected by upstream is retired before a later full-context request. Existing Responses retirement requirements govern the latter; client-supplied anchor authority remains unchanged.

The durable carrier is the user fork's `codex/integrated-runtime` branch. The tracked source manifest enumerates all 90 open-PR inputs from the selection snapshot, with 34 included concerns and explicit dispositions for the other 56. The commit containing the manifest is the source identity; a deployment receipt separately binds that commit to the built image and accepted database backup. This avoids embedding a self-referential commit hash in the source.

The four deployed local migration files retain their exact bytes and parents. The new aggregate merge revision joins the old deployed branch and the repaired current upstream branch. A synthetic upgrade retained accounts, API keys, history participation, usage, retry receipts and operator settings; current upstream intentionally normalizes the old transport sentinel `default` to `auto`. Schema drift and migration policy checks found no differences. A deployment owner must still rehearse the actual populated backup before live acceptance.
