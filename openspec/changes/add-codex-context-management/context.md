# Local integration reconciliation

This integration combines source c75d7f3d34b1ec5d19a557efba160eb2fd7dd522 with local refresh base 9063f4dda7d3c851504f65e6c022a37ab6464abb. Both sides of the additive specification, Makefile and retry-test conflicts are retained. The realtime sideband test keeps one app lifespan, with the selection cache installed on that service. The existing native-control deadline fix remains intact.

The positive dispatch cache retains the accepted deployed behavior. Unmarked cache misses query durable ownership, so a restart or eviction cannot bypass key ownership or drop participation. Ordinary unbound requests remain untracked but incur a lookup. The API regression covers both marked and unmarked foreign-key requests after restart and eviction.

The context migration is byte-identical to effc2a944313fbf3325330785ba3a3d3e95a1076. Its original parent is 20260830_000000_add_quota_warmup_claim_expiry. The external branch reparented that same revision onto a September 8 head; applying that rewrite to an already deployed lineage is forbidden. This branch deliberately does not join the aggregate graph. The integration owner must preserve all historical deployed revisions, including 20260905_140000_merge_retry_claim_and_codex_context_heads and its parents, and add a new merge revision that joins current heads. Migration checks and real database upgrade rehearsal remain required after that composition.

Local verification on 2026-09-09: 180 tests passed across context dispatch cost, context pool, history/notes, context replay, transient retry and realtime-live suites. Both database environment variables pointed to the same dedicated absolute disposable SQLite file at /tmp/codex-lb-context-refresh-proof.sqlite. Main and background engine URLs were asserted before pytest loaded its resetting fixtures. This worktree has its own frozen uv environment.

All 28 changed Python files parse and pass Ruff. Targeted Ty, touched-file formatting, proxy architecture, cancellation safety, strict active change, and strict context/Responses owning specs pass. The timing checker reports three raw timeout and three raw task sites in http_bridge_event_batcher.py, which is byte-unchanged from this branch's integration base. This integration does not claim that inherited check passes. Migration execution remains deferred until the aggregate migration graph is joined. No Docker or live state was accessed.
