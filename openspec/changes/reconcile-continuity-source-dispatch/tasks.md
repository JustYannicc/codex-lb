## 1. Resolve the pinned composition

- [x] 1.1 Preserve the enum resolver, compatibility wrapper, and overflow selector; verify overflow-selector and source-dispatch suites pass.
- [x] 1.2 Preserve compact settlement ownership and transplant the dashboard resilience binding; verify both compact routes observe false-to-true dashboard changes.
- [x] 1.3 Remove retired environment-only transport fields from the existing image-bypass test while retaining its dashboard settings; verify full ty passes.

## 2. Verify and deliver

- [x] 2.1 Verify bound subscription owners bypass source claims and construction, source-owned requests finalize once, sanitized lookup failures do not dispatch, and affected regressions and independent reviews pass on the exact composition.
- [ ] 2.2 Publish with the expected-head lease and obtain current-head hosted CI, CodeRabbit, and Codex results; record exact delivery evidence.
